---
name: "ui-snow"
description: "已废弃的旧 UI skill 入口。仅在需要兼容历史提法 `ui-snow` 时使用，并立即改为引导到 `/speckit.ui`；新的 UI/UE 设计、Figma 同步、参考图分析、设计规范应用、D2C、C2D 一律改由 `speckit` 的 `/speckit.ui` 阶段完成。"
---

# UI Snow (Deprecated)

这个 skill 已废弃。

从现在开始：

- 不再把 `ui-snow` 当作独立的 UI/UE 主 skill
- 不再在这里维护新的设计工作流
- 所有 UI/UE 设计、参考图分析、设计规范约束、`ui.md` 产出、Figma 同步、`D2C`、`C2D`，统一改由 [speckit](/Users/fallin/.codex/skills/speckit/SKILL.md) 的 `/speckit.ui` 阶段完成

如果用户提到：

- `ui-snow`
- `UI设计`
- `UE设计`
- `参考图分析`
- `页面方案`
- `Figma`
- `D2C`
- `C2D`

默认处理方式是：

1. 说明 `ui-snow` 已废弃
2. 改为使用 `speckit` 的 `/speckit.ui`
3. 在 `/speckit.ui` 中完成完整 UI 工作流

不要继续在本 skill 中扩展新规则。
